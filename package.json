{
  "name": "simple-slack-messenger",
  "version": "0.1.0",
  "author": "Aloysius (https://alloystory.github.io/)",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.2.3",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@slack/webhook": "^6.1.0"
  },
  "scripts": {
    "build": "tsc --build --clean && tsc --build"
  }
}
